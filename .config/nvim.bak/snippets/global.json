{
  "fake name": {
    "prefix": "fakename",
    "body": "${VIM:fake#gen('male_name')}"
  },
  "fake fullname": {
    "prefix": "fakefullname",
    "body": "${VIM:fake#gen('male_name')} ${VIM:fake#gen('male_name')}"
  },
  "fake age": {
    "prefix": "fakeage",
    "body": "${VIM:fake#int(15,100)}"
  },
  "fake number": {
    "prefix": "fakeint",
    "body": "${VIM:fake#int(1000)}"
  },
  "fake float": {
    "prefix": "fakefloat",
    "body": "${VIM:fake#float(1000)}"
  },
  "fake password": {
    "prefix": "fakepass",
    "body": "${VIM:fake#chars(10)}"
  },
  "fake email": {
    "prefix": "fakeemail",
    "body": "${VIM:fake#gen('male_name')}.${VIM:fake#gen('male_name')}${VIM:fake#int(99)}@gmail.com"
  },
  "fake country": {
    "prefix": "fakecountry",
    "body": "${VIM:fake#gen('country')}"
  },
  "fake job": {
    "prefix": "fakejob",
    "body": "${VIM:fake#gen('job')}"
  },
  "fake phone": {
    "prefix": "fakephone",
    "body": "01${VIM:fake#choice(['0','1','2','5'])}${VIM:fake#chars(8, '\\d')}"
  }
}
